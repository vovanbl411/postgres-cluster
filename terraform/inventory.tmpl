[bastion]
bastion-host ansible_host=${bastion_ip}
ansible_user=root

[postgres_nodes]
%{ for index, ip in pg_ips ~}
pg-node-${index + 1} ansible_host=${ip}
%{ endfor ~}

[postgres_nodes:vars]
ansible_user=root
ansible_ssh_common_args='-o ProxyJump=root@${bastion_ip}'